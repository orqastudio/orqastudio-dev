---
id: AD-8b91f5a4
title: Persistence Architecture
description: 9 core SQLite tables plus FTS5 virtual tables. Governance artifacts indexed in SQLite but content always read from disk.
status: surpassed
created: 2026-03-02
updated: 2026-03-10
relationships:
  - target: RES-a68430ce
    type: informed-by
    rationale: RES-a68430ce designed the 9-table SQLite schema with FTS5 and the file/DB hybrid boundary where governance files stay authoritative
  - target: RES-3b36e149
    type: informed-by
    rationale: RES-3b36e149 established the codebase scanning approach that determined which project metadata needed SQLite indexing vs filesystem reads
  - target: EPIC-46e5f406
    type: drives
  - target: DOC-2a7f1063
    type: documented-by
  - target: DOC-2f60e3a2
    type: documented-by
  - target: DOC-87d0a2de
    type: documented-by
  - target: DOC-f9c6149e
    type: documented-by
---
## Decision

9 core tables (`projects`, `sessions`, `messages`, `artifacts`, `scanner_results`, `metrics`, `tasks`, `lessons`, `settings`) plus 2 FTS5 virtual tables (`messages_fts`, `artifacts_fts`). Messages stored as one row per content block (not per API message). `rusqlite` for Rust backend operations, `tauri-plugin-sql` for frontend queries and migrations. WAL mode for concurrent access. Governance artifacts (.claude/ files) are metadata-indexed in SQLite but content is always read from disk. Claude-generated handoff summaries on session end from Phase 1. Nullable `user_id` and `last_edited_by` columns included from Phase 1 to support future multi-user expansion without schema migration.

## Rationale

One row per content block enables granular FTS indexing, efficient streaming updates, tool-specific queries, and natural UI component mapping. The hybrid file/DB approach (Obsidian pattern) preserves full CLI compatibility — `.claude/` files are authoritative, SQLite is a derived cache. `notify-debouncer-full` file watcher (500ms debounce) keeps the index in sync. Full session history stored (< 5 GB/year at heavy use). FTS5 cross-session search performs under 50ms at 1M messages. Handoff summaries address the #1 pain point (context loss between sessions) — negligible cost via sidecar. Multi-user columns are nullable and unused in Phase 1, avoiding a migration-heavy schema change when collaborative access is implemented.

## Consequences

Schema managed via numbered SQL migration files in `backend/src-tauri/migrations/`. `.claude/` directory watched with `notify` crate. `orqa.db` added to `.gitignore`. SHA-256 hashes used for change detection to avoid re-indexing unchanged files. Sidecar must support a "generate summary" request type for handoff note generation.
