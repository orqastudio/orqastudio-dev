---
id: AD-e513c9e4
title: Thick Backend Architecture
description: Rust owns the domain model; Svelte is the view layer that renders what Rust tells it.
status: completed
created: 2026-03-02
updated: 2026-03-13
relationships:
  - target: RULE-1acb1602
    type: enforced-by
    rationale: RULE-1acb1602 requires all features to include a Rust command layer, ensuring domain logic cannot migrate to the frontend
  - target: RULE-11c29c9e
    type: enforced-by
    rationale: RULE-11c29c9e enforces skill portability boundaries that keep core and project concerns separated, supporting the thick backend architecture
  - target: DOC-2f60e3a2
    type: documented-by
  - target: DOC-01ddd8aa
    type: documented-by
---
## Decision

Rust owns the domain model — sessions, artifacts, governance state, agent orchestration. Svelte is a view layer that renders what Rust tells it.

## Rationale

The domain logic (session management, artifact parsing, Claude API integration, file system tools) is complex and benefits from Rust's type system, performance, and reliability. Keeping it in the backend means the frontend stays simple and testable.

## Consequences

All business logic lives in `backend/src-tauri/src/domain/`. The frontend never makes decisions about data flow or state management beyond UI concerns.
