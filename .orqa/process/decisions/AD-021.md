---
id: AD-6ce44025
title: .orqa/ as Single Source of Truth
description: All governance artifacts live in .orqa/ as the single source of truth. .claude/ contains only symlinks for CLI compatibility. Supersedes AD-69072318 which positioned .claude/ as the canonical location.\n
status: completed
created: 2026-03-08
updated: 2026-03-13
relationships:
  - target: "RULE-6c0496e0"
    type: "enforced-by"
    rationale: "RULE-6c0496e0 explicitly forbids writing governance artifacts to .claude/ and mandates .orqa/ as source of truth, with symlinks as the CLI compatibility layer"
  - target: EPIC-766e2afa
    type: drives
  - target: RULE-6083347d
    type: enforced-by
  - target: RULE-633e636d
    type: enforced-by
  - target: RULE-e352fd0a
    type: enforced-by
---
## Decision

`.orqa/` is the single source of truth for ALL governance artifacts. The `.claude/` directory exists only for Claude Code CLI compatibility and contains symlinks — never copies — pointing back to `.orqa/` subdirectories. Only `settings.json` and `worktrees/` remain as real files in `.claude/`.

Symlink map:
- `.claude/rules/` → `.orqa/process/rules/`
- `.claude/agents/` → `.orqa/process/agents/`
- `.claude/skills/` → `.orqa/process/skills/`
- `.claude/hooks/` → `.orqa/process/hooks/`
- `.claude/CLAUDE.md` → `.orqa/process/agents/orchestrator.md`

## Rationale

[AD-69072318](AD-69072318) established that governance artifacts use "native Claude Code format" stored in `.claude/`. As OrqaStudio evolved, the `.orqa/` directory structure was created to provide a richer organizational model (documentation/, planning/, governance/, team/) that goes beyond what `.claude/` supports. Having two directories with overlapping content caused governance divergence — agents wrote to `.claude/` while the app scanned `.orqa/`, resulting in inconsistent enforcement (see [IMPL-b3e47100](IMPL-b3e47100)).

Symlinks solve this: a single set of files in `.orqa/`, with `.claude/` providing CLI compatibility transparently.

## Consequences

- All agents MUST write to `.orqa/` paths, never `.claude/`
- The app scans `.orqa/` exclusively
- CLI tools read through symlinks and see the same content
- If symlinks break, fix the symlinks — don't create copies
