---







id: RES-4124820a
title: Branding Research
description: "Branding as a three-layer cross-cutting concern: OrqaStudio's own visual identity, per-project brand adaptation, and brand-aware AI code generation."
status: completed
created: 2026-03-02
updated: 2026-03-07
relationships:
  - target: EPIC-e881130b
    type: guides
    rationale: Research findings informed the design of Vision & Brand Identity
  - target: RES-3b36e149
    type: informed-by
    rationale: "Auto-generated from body text reference"
  - target: RES-27eb7333
    type: informed-by
    rationale: "Auto-generated from body text reference"
  - target: RES-df5560cb
    type: informed-by
    rationale: "Auto-generated from body text reference"
  - target: RES-11bee8a4
    type: informed-by
    rationale: "Auto-generated from body text reference"
  - target: RES-7f87b149
    type: informed-by
    rationale: "Auto-generated from body text reference"
  - target: AD-afc78f6e
    type: informed-by
    rationale: "Auto-generated from body text reference"
  - target: AD-69072318
    type: informed-by
    rationale: "Auto-generated from body text reference"
---
**Date:** 2026-03-02 | **Status:** Complete

Research into branding as a first-class, three-layer cross-cutting concern in OrqaStudio: (1) OrqaStudio's own visual identity, (2) per-project UI adaptation, and (3) brand-aware AI code generation.

---

## Relationship to Other Research

This document addresses the **gaps** not covered elsewhere. The following aspects are already decided and should not be duplicated:

| Aspect | Covered In | Key Decision |
|--------|-----------|--------------|
| Token extraction heuristics | [`onboarding.md`](RES-3b36e149) | Tier 1 scanning for tailwind.config.*, CSS custom properties, component library conventions |
| Token storage & format | [`design-tokens.md`](RES-27eb7333) | JSON in SQLite keyed by shadcn-svelte variable names, OKLCH format |
| Runtime theme application | [`design-tokens.md`](RES-27eb7333) | `document.documentElement.style.setProperty()` + mode-watcher |
| Extraction pipeline | [`design-tokens.md`](RES-27eb7333) | Three-source extraction: CSS :root > Tailwind config > generic vars. `csscolorparser` for conversion |
| Per-project persistence | [`design-tokens.md`](RES-27eb7333) | `project_themes` + `project_theme_overrides` tables in SQLite |
| shadcn-svelte theming mechanics | [`frontend.md`](RES-df5560cb) | CSS custom property convention, component library selection |

---

## Q1: OrqaStudio's Own Brand Identity

> **Authoritative brand direction:** OrqaStudio Dual-Mode Identity Guidelines (v1) supersedes the initial recommendations below. The guidelines define a dual-mode identity system (Product Mode: Industrial Minimal / Marketing Mode: Cyber-Technical), three logo marks with shared spark wedge geometry, and contextual spark colors (`--orqa-ember`, `--orqa-arc`, `--orqa-amber`, `--orqa-success`). The zinc base palette, Inter font stack, and system-preference dark mode decisions below remain valid.

**Question:** What design principles should guide OrqaStudio's visual identity? Color palette, typography, dark mode defaults, and relationship to the shadcn-svelte base theme.

### Design Principles

OrqaStudio occupies a specific niche: a product management tool used by technical people (PMs, tech leads, senior engineers). It is not an IDE, not a design tool, and not a business dashboard. The visual identity must communicate:

1. **Professional authority** -- This tool governs product development. It should feel decisive, not playful.
2. **Technical competence** -- Users are technical. The UI can be information-dense and use developer-familiar patterns (monospace code, keyboard shortcuts, dark mode).
3. **Quiet confidence** -- The tool should recede when project themes are applied. OrqaStudio's brand should not compete with the project being managed.
4. **Clarity over decoration** -- Every pixel serves information delivery. No gradients for the sake of gradients. No illustrations in the chrome.

### What Professional Tools Do

An analysis of tools in OrqaStudio's competitive space reveals a consistent pattern:

| Tool | Base Palette | Accent | Typography | Dark Mode | Key Principle |
|------|-------------|--------|------------|-----------|---------------|
| **Linear** | Desaturated neutrals (Woodsmoke, Oslo Gray, Black Haze) | Indigo (signature, calm authority) | Inter Display (headings), Inter (body) | Default dark, light available | "Distraction-free canvas that lets your work shine" |
| **Notion** | Warm off-whites, light grays | Minimal -- black/white focus | System fonts + custom serif for content | Light default, dark available | "Blank canvas" -- the content IS the brand |
| **Figma** | Light neutral with vibrant accents | Multi-color (tooling uses many colors) | Figma Sans (custom grotesque) | System preference | "Versatile primitives" -- the tool adapts |
| **GitHub** | Cool grays | Green (success), blue (links) | System font stack | System preference | Functional color -- color means something |
| **Slack** | White/gray canvas | Purple (brand) → now more neutral | Lato/system | Both, user choice | Brand recedes in favor of workspace content |

**Key pattern:** Every successful tool in this space uses a **neutral base** with **one or two accent colors**. The accent color is used sparingly -- for primary actions, active states, and brand moments -- not for large surface areas. Dark mode is either the default (Linear, developer-leaning) or follows system preference (Figma, GitHub).

### Color Palette Strategy

**Verdict: Start from shadcn-svelte's zinc base. Add a single signature accent in the indigo-violet range. Do not customize the neutral scale.**

#### Why zinc (not slate, gray, neutral, or stone)

shadcn-svelte offers five neutral base options:

| Base | Character | Risk |
|------|-----------|------|
| **Gray** | True neutral, adaptable | Bland, no personality |
| **Neutral** | Slightly warmer than gray | Can feel generic |
| **Slate** | Blue undertone, modern | Conflicts with project themes that use blue as primary |
| **Stone** | Warm undertone, earthy | Too informal for a PM tool |
| **Zinc** | Cool undertone, minimalistic | Slightly cool but neutral enough to not conflict |

**Zinc wins** because:
1. Its cool undertone reads as "technical" without being cold.
2. It does not have the blue undertone of slate, which would conflict with projects using blue as their primary brand color. When a project's theme applies a blue primary, the blue would bleed into zinc's neutral areas less than it would into slate's already-blue-tinted grays.
3. It is shadcn-svelte's actual default, meaning zero customization of the neutral scale is needed. Less customization means fewer maintenance burdens when shadcn-svelte updates.
4. Linear -- the closest analog to OrqaStudio in the PM-tool space -- uses a very similar cool desaturated neutral base.

#### Signature accent color

**Verdict: Indigo-violet, approximately `oklch(0.55 0.18 280)` (light mode) / `oklch(0.70 0.18 280)` (dark mode).**

Rationale:
1. **Distinctiveness** -- Blue is overused (every SaaS tool uses blue). Pure purple is associated with design tools (Figma) and creative apps. Indigo-violet sits between them -- technical but not generic.
2. **Harmony with zinc** -- Zinc's cool undertone pairs naturally with indigo. The combination reads as "sophisticated developer tool" rather than "corporate software."
3. **Does not conflict with most project brands** -- The most common project brand colors are blue, green, red, and orange. Indigo-violet has low overlap with all of these. When a project theme overrides `--primary`, the visual shift is noticeable and intentional.
4. **Precedent** -- Linear uses indigo as its signature color for exactly these reasons: "calm authority and deep focus."
5. **OKLCH generation** -- Indigo at the 280-degree hue angle in OKLCH produces clean, perceptually uniform variants across the lightness scale.

**Where the accent appears:**
- `--primary` in OrqaStudio's default theme (buttons, links, active tabs, selected items)
- `--ring` (focus rings)
- `--sidebar-primary` (active navigation item)
- Chart accent colors (chart-1)

**Where the accent does NOT appear:**
- Large surface areas (backgrounds, cards, sidebars stay zinc)
- Borders, inputs, muted areas (stay neutral zinc)
- Destructive actions (stay red, as per shadcn convention)

#### Concrete default theme values

```css
/* OrqaStudio default theme -- light mode */
:root {
  --radius: 0.625rem;
  /* Zinc neutrals (shadcn-svelte defaults, unchanged) */
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0 0);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0 0);
  --secondary: oklch(0.97 0 0);
  --secondary-foreground: oklch(0.205 0 0);
  --muted: oklch(0.97 0 0);
  --muted-foreground: oklch(0.556 0 0);
  --accent: oklch(0.97 0 0);
  --accent-foreground: oklch(0.205 0 0);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.922 0 0);
  --input: oklch(0.922 0 0);

  /* OrqaStudio signature indigo-violet accent */
  --primary: oklch(0.55 0.18 280);
  --primary-foreground: oklch(0.985 0 0);
  --ring: oklch(0.55 0.18 280);

  /* Sidebar with subtle indigo presence */
  --sidebar: oklch(0.985 0 0);
  --sidebar-foreground: oklch(0.145 0 0);
  --sidebar-primary: oklch(0.55 0.18 280);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.97 0.005 280);
  --sidebar-accent-foreground: oklch(0.205 0 0);
  --sidebar-border: oklch(0.922 0 0);
  --sidebar-ring: oklch(0.55 0.18 280);

  /* Charts -- indigo-anchored palette */
  --chart-1: oklch(0.55 0.18 280);
  --chart-2: oklch(0.6 0.118 184.704);
  --chart-3: oklch(0.398 0.07 227.392);
  --chart-4: oklch(0.828 0.189 84.429);
  --chart-5: oklch(0.769 0.188 70.08);
}

/* OrqaStudio default theme -- dark mode */
.dark {
  --background: oklch(0.145 0 0);
  --foreground: oklch(0.985 0 0);
  --card: oklch(0.145 0 0);
  --card-foreground: oklch(0.985 0 0);
  --popover: oklch(0.145 0 0);
  --popover-foreground: oklch(0.985 0 0);
  --secondary: oklch(0.269 0 0);
  --secondary-foreground: oklch(0.985 0 0);
  --muted: oklch(0.269 0 0);
  --muted-foreground: oklch(0.708 0 0);
  --accent: oklch(0.269 0 0);
  --accent-foreground: oklch(0.985 0 0);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.269 0 0);
  --input: oklch(0.269 0 0);

  /* Indigo lightens in dark mode for adequate contrast */
  --primary: oklch(0.70 0.18 280);
  --primary-foreground: oklch(0.15 0.02 280);
  --ring: oklch(0.50 0.15 280);

  --sidebar: oklch(0.205 0 0);
  --sidebar-foreground: oklch(0.985 0 0);
  --sidebar-primary: oklch(0.70 0.18 280);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.269 0.01 280);
  --sidebar-accent-foreground: oklch(0.985 0 0);
  --sidebar-border: oklch(0.269 0 0);
  --sidebar-ring: oklch(0.50 0.15 280);

  --chart-1: oklch(0.70 0.18 280);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
}
```

The key difference from stock shadcn-svelte zinc: `--primary`, `--ring`, `--sidebar-primary`, and `--chart-1` shift from achromatic zinc to indigo-violet. Everything else stays untouched. This means OrqaStudio has a recognizable brand color while remaining neutral enough for project themes to dominate when applied.

### Typography

**Verdict: System font stack with Inter as the preferred face. No bundled fonts.**

#### Why system fonts over bundled fonts

| Approach | Pros | Cons |
|----------|------|------|
| **System fonts** | Zero load time, native feel, no FOUT, smaller bundle | Less distinctive, varies by OS |
| **Bundled font (e.g., Inter)** | Consistent across platforms, more control | +200-400 KB bundle, FOUT possible, maintenance burden |
| **Custom font (like Figma Sans)** | Maximum brand distinction | Licensing cost, large bundle, overkill for OrqaStudio |

For a Tauri desktop app, system fonts are the best choice:

1. **Desktop apps should feel native.** A Tauri app using the platform's native font feels like a first-class desktop citizen, not a website in a frame.
2. **Inter is already a system font on many developer machines.** It ships with many Linux distributions, is commonly installed by developers on macOS and Windows, and is the default for many developer tools (VS Code, Linear).
3. **No FOUT.** Bundled fonts in a Tauri WebView can flash unstyled text during app startup. System fonts render immediately.
4. **OrqaStudio is not a brand-forward consumer app.** It does not need its own typeface for marketing or consumer recognition. It is a professional tool where typography should be invisible -- legible, information-dense, and fast.

#### Font stack declaration

```css
:root {
  /* Body text: neo-grotesque system font stack */
  --font-sans: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans',
    Arial, sans-serif;

  /* Code and technical content */
  --font-mono: 'JetBrains Mono', 'Cascadia Code', 'Fira Code', Menlo, Monaco,
    'Courier New', monospace;
}
```

**Why this order:**
- **Inter first** -- The modern standard for technical UI. Used by Linear, Vercel, and most shadcn-based apps. If installed (common on dev machines), it provides the ideal look.
- **Roboto** -- Ships with ChromeOS and many Linux distros. Strong fallback.
- **Helvetica Neue / Arial Nova** -- Platform defaults for macOS and Windows respectively.
- **sans-serif** -- Ultimate fallback.

For monospace, JetBrains Mono and Cascadia Code are the modern standards for developer tools. Both support ligatures. Fira Code is the fallback for Linux systems.

#### Why not Inter Display for headings (like Linear)

Linear uses Inter Display for headings because it has slightly different optical sizing for large text. This is a reasonable choice but adds complexity:
- Inter Display requires a separate font file or variable font axis
- The visual difference at OrqaStudio's heading sizes (16-24px) is marginal
- Inter's variable font axis (`font-variation-settings: 'opsz' 28`) can achieve a similar effect without a separate file

**Decision:** Use standard Inter with `font-variation-settings` for optical sizing at large sizes if Inter's variable font is available. This is a zero-cost enhancement that degrades gracefully.

### Dark Mode Default

**Verdict: Follow system preference. Default to dark if system preference is unavailable.**

| Option | Audience Signal | Precedent |
|--------|----------------|-----------|
| **Dark default** | "This is a developer tool" | Linear, Terminal, VS Code |
| **Light default** | "This is a business/PM tool" | Notion, Jira, Asana |
| **System preference** | "We respect your setup" | Figma, GitHub, macOS apps |

OrqaStudio straddles the developer/PM line. Its primary persona (Alex, PM/Tech Lead) works in both contexts. System preference is the safest default because:

1. It respects the user's existing environment. A PM using light mode everywhere will not be jarred. A developer using dark mode everywhere will feel at home.
2. `mode-watcher` (already decided in design-tokens.md) handles system preference detection, manual toggle, and persistence out of the box.
3. If system preference is not available (rare, but possible on some Linux WMs), dark mode is the better fallback because OrqaStudio's primary user base skews technical.

**Implementation:** Already covered in [`design-tokens.md`](RES-27eb7333) Q2. `mode-watcher` with `ModeWatcher` component in root layout. Default mode: `'system'`. Fallback: `'dark'`.

---

## Q2: Design System as Governance Artifact

**Question:** Should a project's design system be a `.claude/` governance artifact? What format? How does it relate to existing artifact types? Should OrqaStudio auto-generate it?

### The Case for Design System as a Governance Artifact

The `.claude/` directory already contains rules that govern how Claude behaves when working on a project. A design system is exactly this kind of governance -- it constrains how code should be written. When Claude generates frontend code, it needs to know:

- Which CSS variables to use (`bg-primary` not `bg-blue-600`)
- Which component library conventions to follow (shadcn vs Chakra vs custom)
- What the project's color palette is (so generated mockups use real brand colors)
- Typography and spacing conventions

This information is no different from a coding standard or architectural rule. It belongs in `.claude/rules/`.

### How Other Tools Handle This

| Tool | Approach | Format |
|------|----------|--------|
| **v0.dev** | Registry specification. Components, blocks, and design tokens in a `registry.json`. CSS variables follow shadcn convention. Tokens in `tokens.css`. | JSON (registry) + CSS (tokens) |
| **Cursor** | `.cursor/rules/*.mdc` files with YAML frontmatter for path scoping. Frontend rules specify component conventions, styling approaches, design patterns. | Markdown + YAML frontmatter |
| **Claude Code** | `.claude/rules/*.md` files. Auto-loaded into context. Path-scoped via `paths:` frontmatter. | Markdown + YAML frontmatter |
| **Storybook** | Component documentation co-located with code. Design tokens documented as stories. | JSX/TSX stories |
| **Zeroheight** | Living documentation platform. Syncs with Figma and Storybook. Manages tokens via W3C DTCG spec. | Web platform (DTCG JSON for tokens) |
| **Style Dictionary** | JSON/YAML token definitions. Build system transforms tokens to platform-specific outputs (CSS vars, iOS, Android). First-class DTCG support in v4. | JSON/YAML (DTCG format) |

**Key insight from v0.dev:** Vercel's approach with registries demonstrates that the most effective way to give AI design system context is through a structured specification that maps directly to the component library's conventions. Their registry uses the shadcn CSS variable naming convention -- exactly what OrqaStudio already uses internally.

**Key insight from Cursor:** Path-scoped rules are the correct pattern. A design system rule should only be injected into context when Claude is working on frontend files, not when it is writing Rust backend code or SQL migrations.

### Recommended Artifact Format

**Verdict: `.claude/rules/design-system.md` -- a markdown file with YAML frontmatter, scoped to frontend file paths.**

This format is chosen because:

1. **CLI compatibility** -- It is a standard `.claude/rules/` file. Claude Code CLI will automatically load it when working in scoped paths. No OrqaStudio-specific format needed.
2. **Path scoping** -- The `paths:` frontmatter field limits injection to frontend work, avoiding wasted context tokens on backend conversations.
3. **Human-readable** -- PMs and designers can read and edit the file without understanding JSON schemas.
4. **Machine-parseable** -- YAML frontmatter contains structured token data that OrqaStudio can extract programmatically. The markdown body contains natural language instructions that Claude consumes directly.
5. **Git-friendly** -- Lives in the repo. Version-controlled. Reviewed in PRs.

#### File structure

```markdown
---
paths:
  - "src/**/*.svelte"
  - "src/**/*.ts"
  - "src/**/*.css"
  - "src/**/*.tsx"
  - "src/**/*.jsx"
  - "app/**/*.tsx"
  - "app/**/*.css"
  - "components/**/*"
  - "styles/**/*"
tokens:
  primary: "#1a1a2e"
  primary-foreground: "#fafafa"
  secondary: "#e2e2e8"
  accent: "#16a34a"
  background: "#ffffff"
  foreground: "#0a0a0a"
  destructive: "#dc2626"
component-library: shadcn-svelte
css-framework: tailwind-v4
---
# Design System

## Color Usage

- Use semantic Tailwind classes: `bg-primary`, `text-primary-foreground`, `border-border`
- NEVER use raw color values like `bg-blue-600` or `text-[#1a1a2e]`
- NEVER use Tailwind's default palette colors (blue-500, red-600, etc.) for brand elements
- Use `destructive` for error states and delete actions
- Use `muted` and `muted-foreground` for secondary text and disabled states

## Component Library

This project uses shadcn-svelte (Svelte 5). When generating UI code:

- Import components from `$lib/components/ui/` (e.g., `import { Button } from "$lib/components/ui/button"`)
- Use the component variants as documented (e.g., `<Button variant="destructive">`)
- Do not create custom components for functionality that shadcn-svelte provides
- Follow the shadcn-svelte patterns for forms, dialogs, and data display

## Typography

- Body text uses the default sans-serif stack (no explicit font classes needed)
- Code blocks use `font-mono`
- Headings use `font-semibold` or `font-bold`, never `font-black`

## Spacing

- Use Tailwind's default spacing scale (4px increments)
- Card padding: `p-6`
- Section gaps: `space-y-4` or `gap-4`
- Page margins: `px-4 md:px-6`
```

### Relationship to Existing Artifact Types

| Artifact Type | Purpose | Example |
|---------------|---------|---------|
| **Agents** | Define specialized AI personas with specific tools and expertise | `backend-engineer.md`, `qa-reviewer.md` |
| **Rules** | Constrain how Claude writes code -- style, patterns, conventions | `coding-standards.md`, `testing.md` |
| **Skills** | Multi-step procedures Claude can execute | `deploy-to-staging.md` |
| **Hooks** | Pre/post actions triggered by tool use | `pre-commit-check.sh` |
| **Design System (new)** | **A specialized rule** that constrains UI code generation | `design-system.md` |

The design system artifact is a **rule**, not a new artifact type. It lives in `.claude/rules/` because it governs code generation behavior -- exactly what rules do. The only special treatment is:

1. **Auto-generation** -- OrqaStudio can auto-generate this file during codebase scan (Tier 1 heuristics)
2. **OrqaStudio UI integration** -- OrqaStudio reads the `tokens:` frontmatter to apply per-project theming (in addition to the token extraction pipeline from `design-tokens.md`)
3. **Path scoping** -- Scoped to frontend paths so it does not waste context on backend work

### Auto-Generation During Codebase Scan

**Verdict: Yes. OrqaStudio should auto-generate `.claude/rules/design-system.md` if design tokens are detected during codebase scan AND the file does not already exist.**

The auto-generation flow:

1. **Tier 1 scan** detects design tokens (tailwind.config, CSS custom properties, component library dependencies)
2. OrqaStudio checks if `.claude/rules/design-system.md` exists
3. If it does not exist, OrqaStudio generates it from extracted tokens:
   - `tokens:` frontmatter populated from extracted values
   - `component-library:` set based on detected dependencies (shadcn, Chakra, Radix, etc.)
   - `css-framework:` set based on detected framework (tailwind-v3, tailwind-v4, vanilla-css)
   - `paths:` set based on detected frontend directory structure
   - Markdown body contains standard usage rules appropriate to the detected stack
4. OrqaStudio presents the generated file to the user for review before writing to disk: "I detected design tokens in your project. Would you like me to create a design system rule for Claude?"
5. User can edit, approve, or dismiss

**If the file already exists**, OrqaStudio reads it as an additional source for per-project theming (the `tokens:` frontmatter). OrqaStudio never overwrites an existing file.

**Why require user approval:** Auto-generating governance artifacts without user consent violates the progressive disclosure principle from [`onboarding.md`](RES-3b36e149). The user should feel in control of what goes into `.claude/`.

### How the Artifact Gets Surfaced to Claude

The design system rule is surfaced to Claude through the standard `.claude/rules/` mechanism:

1. **Claude Code CLI (direct):** The CLI automatically loads all `.claude/rules/*.md` files. Path-scoped rules are only loaded when Claude is working with matching files. No OrqaStudio-specific logic needed -- this is built into the CLI.

2. **OrqaStudio's Agent SDK sidecar (via system prompt):** When OrqaStudio constructs a `ConversationRequest` to send to the sidecar, it includes relevant rules in the system prompt. OrqaStudio's rule injection logic:
   - Reads all `.claude/rules/*.md` files
   - For each rule, checks the `paths:` frontmatter against the files currently being discussed in the conversation
   - If the conversation involves frontend files (detected by file extensions or explicit user intent), the design system rule is included
   - If the conversation is purely backend, the design system rule is excluded

This is the same mechanism used for all rules. The design system rule is not special in how it is delivered -- only in how it is generated and how OrqaStudio uses its `tokens:` frontmatter for UI theming.

---

## Q3: Brand-Aware Code Generation

**Question:** How should design tokens be surfaced to Claude during conversations? How does Claude know to use `bg-primary` instead of `bg-blue-600`? What does the system prompt section look like?

### The Problem

When Claude generates frontend code without design system context, it defaults to:
- Raw Tailwind colors (`bg-blue-600`, `text-gray-900`)
- Hardcoded hex values (`style="color: #1a1a2e"`)
- Generic component patterns that do not match the project's library
- Inconsistent spacing, typography, and layout conventions

This produces code that works but does not integrate with the project's design system. The developer must manually replace every color reference, swap generic HTML for component library calls, and fix spacing/typography to match conventions. This defeats the purpose of AI-assisted code generation.

### How Other Tools Solve This

#### v0.dev (Vercel)

v0.dev's approach is the most mature:

1. **Registry specification** -- A structured JSON file (`registry.json`) describes the project's components, their file locations, dependencies, and styling.
2. **Token file** -- A `tokens.css` file defines all CSS variables following the shadcn CSS variable convention. v0 instructs: "overwrite the colors in the file with your custom theme ensuring all the variable names remain unchanged."
3. **Default stack** -- When no registry is provided, v0 defaults to shadcn/ui + Tailwind CSS + Lucide icons. This means generated code always targets a known component library.
4. **Registry as context** -- The registry is described as "a distribution specification designed to pass context from your design system to AI Models." The AI model receives the registry and generates code that references the project's actual components and tokens.

**Lesson for OrqaStudio:** The most effective approach is to give Claude a compact, structured specification of the project's design system -- not the entire design system documentation, but a focused summary of what to use and what to avoid.

#### Cursor

Cursor uses `.cursor/rules/*.mdc` files with path scoping. Frontend rules specify:
- Component naming conventions (PascalCase, prop interfaces)
- Styling approach (Tailwind classes, CSS modules, etc.)
- Component library usage patterns
- State management conventions

**Lesson for OrqaStudio:** Path scoping is critical. Design system context should only be injected when the conversation involves frontend files. Injecting it during backend work wastes context tokens and clutters the prompt.

#### Claude Code CLI

Claude Code uses `.claude/rules/*.md` with path-based scoping via `paths:` frontmatter. Rules are automatically loaded when Claude is working with matching files.

**Lesson for OrqaStudio:** The existing `.claude/rules/` mechanism is sufficient. No new delivery mechanism is needed.

### System Prompt Design for Design Awareness

**Verdict: A compact design token context block injected into the system prompt when the conversation touches frontend code.**

#### When to inject

Design system context should be injected when ANY of the following conditions are true:

1. **Explicit frontend files in the conversation** -- The user mentions or opens files with extensions: `.svelte`, `.tsx`, `.jsx`, `.vue`, `.css`, `.scss`, `.html`
2. **Frontend-related user intent** -- The user asks to "build a component," "create a page," "design a form," "add a button," etc.
3. **The design system rule exists** -- `.claude/rules/design-system.md` is present and has matching path patterns

When NONE of these conditions are true (e.g., the user is debugging a Rust function or writing SQL), the design system context is **not injected** to conserve context window tokens.

#### System prompt section format

The design system context is injected as a clearly delineated section within the system prompt:

```
<design-system>
## Project Design System

### Tokens (CSS Custom Properties)
- --primary: #1a1a2e (brand color — use for CTAs, active states, links)
- --primary-foreground: #fafafa (text on primary backgrounds)
- --secondary: #e2e2e8 (secondary surfaces)
- --accent: #16a34a (highlights, success states)
- --background: #ffffff (page background)
- --foreground: #0a0a0a (default text)
- --destructive: #dc2626 (errors, delete actions)
- --muted: #f4f4f5 (subdued backgrounds)
- --muted-foreground: #71717a (subdued text)
- --border: #e4e4e7 (borders, dividers)

### Component Library: shadcn-svelte
- Import from: $lib/components/ui/{component}
- Available components: Button, Card, Dialog, Input, Select, Tabs, Badge, ...
- Variants: default, destructive, outline, secondary, ghost, link
- Use component library primitives. Do NOT create custom components for
  functionality that the library provides.

### Styling Rules
- ALWAYS use semantic Tailwind classes: bg-primary, text-foreground, border-border
- NEVER use raw palette colors: bg-blue-600, text-gray-900, etc.
- NEVER use hardcoded hex/rgb values in class attributes or inline styles
- Use the spacing scale: gap-4, p-6, space-y-4 (4px increments)
- Dark mode is handled by CSS variables — do NOT add dark: variants for colors

### CSS Framework: Tailwind v4
- Theme registered via @theme inline in app.css
- Utility classes reference CSS custom properties automatically
</design-system>
```

#### Why this format

1. **XML-delimited** -- The `<design-system>` tags make the section unambiguous for Claude to identify and prioritize. Claude reliably follows instructions within clearly delimited sections.
2. **Compact** -- The entire block is approximately 400-500 tokens. This is small enough to include in every frontend-related conversation without significant context window cost.
3. **Actionable** -- Each line is a rule Claude can follow. "NEVER use raw palette colors" is more effective than "the project's palette is defined in tailwind.config.ts."
4. **Token values included** -- Including the actual color values (not just variable names) helps Claude make informed design decisions. If `--primary` is indigo, Claude can choose complementary placeholder images, suggest appropriate contrast ratios, etc.
5. **Component library specified** -- Naming the library and its import convention eliminates the most common error: Claude importing from the wrong package or using raw HTML instead of component library primitives.

### Conditional Injection Implementation

```typescript
// In the system prompt builder (Rust backend, simplified as TypeScript pseudocode)

function buildSystemPrompt(
  basePrompt: string,
  conversationContext: ConversationContext,
  projectRules: Rule[],
): string {
  let prompt = basePrompt;

  // Inject rules that match the current conversation context
  for (const rule of projectRules) {
    if (rule.paths && rule.paths.length > 0) {
      // Path-scoped rule: only inject if conversation touches matching files
      const matchesContext = conversationContext.activeFiles.some(file =>
        rule.paths.some(pattern => matchGlob(pattern, file))
      );

      if (!matchesContext && !conversationContext.hasFrontendIntent) {
        continue; // Skip this rule -- not relevant to current conversation
      }
    }

    prompt += '\n\n' + rule.content;
  }

  return prompt;
}
```

### Frontend Intent Detection

Determining whether a conversation has "frontend intent" when no files are explicitly mentioned:

```rust
/// Keywords that indicate frontend intent in user messages
const FRONTEND_INTENT_KEYWORDS: &[&str] = &[
    "component", "button", "form", "page", "layout", "modal", "dialog",
    "sidebar", "navbar", "header", "footer", "card", "table", "list",
    "grid", "flex", "responsive", "mobile", "dark mode", "theme",
    "style", "css", "tailwind", "color", "font", "spacing", "padding",
    "margin", "border", "shadow", "animation", "transition", "hover",
    "icon", "image", "svg", "ui", "ux", "wireframe", "mockup",
    "design", "brand", "palette", "typography",
];

fn has_frontend_intent(message: &str) -> bool {
    let lower = message.to_lowercase();
    FRONTEND_INTENT_KEYWORDS.iter().any(|kw| lower.contains(kw))
}
```

This is a heuristic, not a classifier. False positives (injecting design context when not needed) waste ~500 tokens -- a minor cost. False negatives (not injecting when needed) produce incorrect code -- a major cost. The heuristic is intentionally aggressive: when in doubt, inject.

### Component Library Convention Communication

Different component libraries have different conventions. The design system rule should include library-specific instructions. OrqaStudio can auto-generate these based on the detected component library:

| Library | Key Conventions |
|---------|----------------|
| **shadcn-svelte** | Import from `$lib/components/ui/`. Variants via `variant` prop. Styling via CSS variables. |
| **shadcn (React)** | Import from `@/components/ui/`. Variants via `variant` prop. `cn()` for class merging. |
| **Radix UI** | Unstyled primitives. Must add Tailwind/CSS classes. Composed from multiple sub-components. |
| **Chakra UI** | Style props (`bg="blue.500"`). Theme tokens via `useTheme()`. Responsive arrays. |
| **MUI** | `sx` prop for styling. Theme via `ThemeProvider`. Slots API for customization. |
| **Mantine** | CSS Modules by default. `styles` prop for overrides. Theme via `MantineProvider`. |

OrqaStudio generates the appropriate convention block based on `component-library:` in the design system rule frontmatter. If no library is detected, a generic "use semantic CSS variables" instruction is used.

### Design-System-Aware Agent

**Verdict: Yes, create a specialized agent for dedicated UI work, but not for Phase 1.**

A design-system-aware agent would be an agent definition in `.claude/agents/` specifically tuned for UI code generation:

```markdown
---
name: UI Engineer
description: Generates frontend components that strictly follow the project's design system.
tools: [Read, Write, Edit, Glob, Grep]
---
# UI Engineer Agent

You are a frontend specialist. You generate UI code that strictly adheres to
the project's design system. You NEVER use raw color values, ALWAYS use the
component library, and ALWAYS follow the spacing/typography conventions.

Before generating any component:
1. Read the project's design system rule (.claude/rules/design-system.md)
2. Check existing components for patterns and conventions
3. Identify the component library and its import conventions
4. Generate code that uses semantic tokens exclusively

After generating code:
1. Verify no raw color values (hex, rgb, hsl) appear in the output
2. Verify all imports use the correct component library paths
3. Verify spacing follows the project's scale
```

**Why defer to post-Phase 1:** Phase 1 does not include agent delegation (the ability to route conversations to specialized agents). The design system rule in `.claude/rules/` provides adequate design awareness for Phase 1's single-agent conversations. The specialized agent becomes valuable in Phase 2+ when OrqaStudio supports multi-agent workflows.

---

## Q4: Brand Hierarchy & Conflict Resolution

**Question:** When OrqaStudio's brand, a project's brand, and a component library's conventions conflict, what wins? How does theming work across multiple projects?

### The Three-Tier Precedence Model

When there is a conflict between branding layers, the resolution follows this precedence (highest to lowest):

```
1. Component Library Conventions  (highest -- structural, not optional)
2. Project's Design Tokens         (the project's actual brand)
3. OrqaStudio's Default Theme            (lowest -- the fallback)
```

#### Why this order

**Component library conventions win** because they are structural, not aesthetic. If shadcn-svelte expects `--primary` to be a single color value applied via `bg-primary`, you cannot override this convention without breaking every component. Library conventions dictate how tokens are consumed, not what values they have. This is non-negotiable.

**Project tokens override OrqaStudio defaults** because the entire purpose of per-project theming is to make OrqaStudio feel like a companion to the project. If the user opens a project with a red brand, OrqaStudio should be red, not indigo. OrqaStudio's defaults are literally fallbacks for when project tokens are not available.

**OrqaStudio's defaults are the floor** -- they provide a complete, coherent theme when no project is open or when the project has no detectable design tokens.

### Concrete Conflict Scenarios

| Scenario | Resolution |
|----------|------------|
| Project uses `--primary: red` but OrqaStudio's default is indigo | **Project wins.** OrqaStudio's UI turns red. |
| Project uses `--primary: red` but shadcn-svelte expects `--primary` to be a CSS color value, and the project provides an invalid value | **OrqaStudio default wins.** Invalid values are discarded; fallback to indigo. |
| Project uses Chakra UI (no CSS variables, uses `bg="red.500"` pattern) but OrqaStudio's chrome uses shadcn-svelte | **OrqaStudio chrome uses OrqaStudio defaults.** The project's Chakra tokens cannot map to shadcn-svelte variables. OrqaStudio's theming adapts what it can (extracted color values mapped to `--primary`), but OrqaStudio's own UI remains shadcn-based. |
| User manually overrides `--primary` to green in project settings | **User override wins over extraction.** This is already implemented in `project_theme_overrides` (see design-tokens.md Q4). |
| Project has a `.claude/rules/design-system.md` that says "use bg-blue-600" but the project's tokens have `--primary: red` | **The rule wins for Claude's code generation** (Claude follows the rule text). **The tokens win for OrqaStudio's UI** (OrqaStudio reads `tokens:` frontmatter for theming). This is an inconsistency in the project's governance, not a OrqaStudio bug. |

### Multi-Project Theming (Future -- Phase 2+)

**Current state (Phase 1):** OrqaStudio has a single active project at a time. When the user switches projects, the old theme is cleared and the new project's theme is applied. This is simple and avoids any multi-project theming complexity.

**Future state (Phase 2+):** When OrqaStudio supports multiple open projects (tabs or sidebar), each project's conversation panel could theoretically use its own theme. This raises the question: does the entire OrqaStudio chrome change, or only the content area?

**Verdict: Chrome stays OrqaStudio-branded. Content area adopts project theme. Deferred to Phase 2+ design.**

The recommended approach for multi-project theming:

1. **OrqaStudio chrome** (sidebar, header, settings) always uses OrqaStudio's default theme. This provides visual stability -- the user always knows they are in OrqaStudio.
2. **Content area** (conversation panel, artifact viewer, detail panel) adopts the active project's theme. The active project is the one currently focused.
3. **Visual indicator** -- A small color swatch or badge in the project tab shows the project's primary color, helping users distinguish projects even when the content area is not themed.
4. **Theme transition** -- When switching projects, the content area's CSS variables are updated. This is the same `applyProjectTheme()` mechanism from design-tokens.md, just scoped to a content container instead of `:root`.

**Implementation for future multi-project scoping:**

```css
/* Instead of applying to :root, apply to a content container */
.project-content[data-project-id="123"] {
  --primary: oklch(0.55 0.20 15);   /* Project's red */
  --primary-foreground: oklch(0.98 0 0);
  /* ... */
}

.project-content[data-project-id="456"] {
  --primary: oklch(0.50 0.18 145);  /* Project's green */
  --primary-foreground: oklch(0.98 0 0);
  /* ... */
}
```

This is a CSS-only change -- shadow-free, no JavaScript per frame, and no performance concern. shadcn-svelte components inside the scoped container will inherit the project's variables automatically because CSS custom properties cascade.

### User Control: Disabling Per-Project Theming

**Verdict: Yes, users should be able to disable per-project theming. Both globally and per-project.**

| Setting | Location | Effect |
|---------|----------|--------|
| **Global toggle** | Settings > Appearance > "Adapt UI to project theme" | When OFF, OrqaStudio always uses its default theme regardless of detected tokens. |
| **Per-project toggle** | Project Settings > Theme > "Use detected theme" | When OFF, this specific project uses OrqaStudio defaults. Other projects still adapt. |
| **Per-project override** | Project Settings > Theme > Token overrides | User can tweak individual tokens. Already implemented in `project_theme_overrides`. |

Global toggle default: **ON** (per-project theming is a headline feature -- users should experience it by default).

### Wireframe Generation and Brand

**Verdict: Wireframes always use the project's brand, never OrqaStudio's brand.**

When Claude generates wireframes for a project (PlantUML Salt diagrams, as decided in [`wireframing.md`](RES-11bee8a4)), the wireframes should represent the **project's** interface, not OrqaStudio's interface. This means:

1. **Color tokens** -- If the project has a detected/declared design system, wireframe annotations should reference the project's token names and values.
2. **Component library** -- Wireframes should show the project's component library patterns (e.g., shadcn button variants), not generic HTML elements.
3. **OrqaStudio as invisible tool** -- OrqaStudio is the tool generating the wireframe, not the product being wireframed. Just as Figma does not inject its own brand into designs created within it, OrqaStudio should not inject its brand into wireframes.

The design system context injected into Claude's prompt (Q3) handles this automatically: when Claude generates wireframes, it has the project's design system context and will use project-appropriate tokens and components.

**Exception:** If no project design system is detected (empty project, no tokens), wireframes fall back to generic patterns (neutral colors, standard HTML elements). They do not use OrqaStudio's indigo-violet brand in wireframes.

---

## Summary

| Question | Recommendation | Key Rationale |
|----------|---------------|---------------|
| Q1: Brand Identity | Zinc base + indigo-violet accent. System fonts (Inter-first). System-preference dark mode. | Neutral enough for project themes. Distinctive enough to be recognizable. Linear precedent. |
| Q2: Governance Artifact | Auto-generated `.claude/rules/design-system.md` with YAML frontmatter tokens + markdown usage rules. Path-scoped to frontend files. | CLI-compatible. Human-readable. Machine-parseable tokens. Git-friendly. |
| Q3: Code Generation | Compact `<design-system>` block in system prompt. Conditional injection based on frontend file context or intent keywords. ~500 tokens. | Prevents raw color values. Ensures component library usage. Low context cost. |
| Q4: Conflict Resolution | Library conventions > project tokens > OrqaStudio defaults. User toggles for theming. Wireframes use project brand. Multi-project deferred. | Clear precedence eliminates ambiguity. User stays in control. |

### Key Implementation Artifacts

| Artifact | Phase | Description |
|----------|-------|-------------|
| OrqaStudio default CSS theme | Phase 0d | Zinc + indigo-violet CSS variable declarations for light/dark |
| Font stack declaration | Phase 0d | `--font-sans` and `--font-mono` CSS custom properties |
| Design system rule auto-generator | Phase 1 | Rust function that produces `.claude/rules/design-system.md` from extracted tokens |
| System prompt design system block | Phase 1 | Template for the `<design-system>` prompt section |
| Frontend intent detector | Phase 1 | Keyword heuristic for conditional prompt injection |
| UI Engineer agent template | Phase 2+ | `.claude/agents/ui-engineer.md` for dedicated UI code generation |
| Multi-project theme scoping | Phase 2+ | CSS container scoping for per-project content areas |

---

## Related

- [Design Tokens Research](RES-27eb7333) -- Token format, storage, runtime application, extraction pipeline
- [Onboarding Research](RES-3b36e149) -- Codebase scanning heuristics, design system detection
- [Frontend Research](RES-df5560cb) -- shadcn-svelte theming, component library selection
- [Wireframing Research](RES-11bee8a4) -- PlantUML Salt for wireframe generation
- [Claude Integration Research](RES-7f87b149) -- System prompt construction, Agent SDK sidecar
- Architecture Decisions -- [AD-afc78f6e](AD-afc78f6e) (frontend libraries), [AD-69072318](AD-69072318) (governance format)
- MVP Specification -- F-001 (project scan), F-009 (settings)

## Sources

- [Linear Brand Guidelines](https://linear.app/brand) -- Color palette, typography, visual identity
- [How Linear Redesigned Their UI](https://linear.app/now/how-we-redesigned-the-linear-ui) -- LCH color space, surface elevation system
- [The Rise of Linear-Style Design (Medium)](https://medium.com/design-bootcamp/the-rise-of-linear-style-design-origins-trends-and-techniques-4fd96aab7646) -- Design trend analysis
- [Figma Brand Refresh](https://www.figma.com/blog/figma-on-figma-evolving-our-visual-language/) -- Brand evolution principles
- [Modern Font Stacks (GitHub)](https://github.com/system-fonts/modern-font-stacks) -- System font stack classifications
- [System Font Stack](https://systemfontstack.com/) -- Cross-platform system font recommendations
- [Modern Font Stacks](https://modernfontstacks.com/) -- Neo-grotesque and monospace stacks
- [shadcn/ui Theming](https://ui.shadcn.com/docs/theming) -- CSS variable convention, neutral color bases
- [shadcn/ui Themes](https://ui.shadcn.com/themes) -- Color palette picker, base color options
- [shadcn Neutral Colors (shadcndesign)](https://www.shadcndesign.com/academy/neutral-colors) -- Zinc, slate, gray, stone, neutral comparison
- [v0.dev Design Systems](https://v0.app/docs/design-systems) -- Registry specification, token format, AI-native design system context
- [AI-Powered Prototyping with Design Systems (Vercel)](https://vercel.com/blog/ai-powered-prototyping-with-design-systems) -- Registry as AI context
- [Cursor Rules Guide](https://design.dev/guides/cursor-rules/) -- .cursor/rules format, path scoping
- [Cursor Directory: Design Rules](https://cursor.directory/rules/design) -- Community design system rules
- [W3C DTCG Specification (2025.10)](https://www.w3.org/community/design-tokens/2025/10/28/design-tokens-specification-reaches-first-stable-version/) -- First stable design token standard
- [Style Dictionary DTCG Support](https://styledictionary.com/info/dtcg/) -- DTCG format in Style Dictionary v4
- [Zeroheight Design Token Manager](https://zeroheight.com/help/article/manage-design-tokens/) -- Design token governance platform
- [Multi-Brand Design Systems (Brad Frost)](https://bradfrost.com/blog/post/the-many-faces-of-themeable-design-systems/) -- Multi-brand theming patterns
- [Multi-Brand Color System (Cieden)](https://cieden.com/book/sub-atomic/color/multibrand-app-color-system) -- Brand hierarchy in multi-brand apps
- [Multi-Brand Design System with Tokens (Sigma)](https://www.thesigma.co/journal/multi-brand-design-system) -- Token layering for multi-brand
- [Claude Code Best Practices](https://code.claude.com/docs/en/best-practices) -- CLAUDE.md structure, rule organization
- [Claude Code Memory (Rules)](https://code.claude.com/docs/en/memory) -- .claude/rules/ path scoping with YAML frontmatter
- [Modular Rules in Claude Code](https://claude-blog.setec.rs/blog/claude-code-rules-directory) -- .claude/rules/ directory structure
- [Builder.io CLAUDE.md Guide](https://www.builder.io/blog/claude-md-guide) -- Best practices for CLAUDE.md content
- [Design System Governance Tooling (Ben Callahan)](https://bencallahan.com/design-system-tooling) -- Governance frameworks for design systems
