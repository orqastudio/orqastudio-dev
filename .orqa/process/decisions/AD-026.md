---
id: AD-6cd1ff6f
title: Domain Service Extraction Pattern
description: "Tauri command handlers are thin orchestrators. Domain logic lives in focused service modules under domain/. Commands delegate to services, services delegate to repositories. No business logic in command files.\n"
status: completed
created: 2026-03-06
updated: 2026-03-13
relationships:
  - target: "RULE-b49142be"
    type: "enforced-by"
    rationale: "RULE-b49142be's clippy::too_many_lines enforcement mechanically prevents command handlers from growing large — long handlers indicate business logic that belongs in domain services"
  - target: EPIC-897bbe8f
    type: drives
---
## Decision

Tauri `#[tauri::command]` handlers follow a strict three-layer pattern:

1. **Command layer** (`commands/*.rs`) — Thin handlers that extract parameters, delegate to domain services, and return IPC-serializable results. Target: 30-50 lines per command function.
2. **Domain layer** (`domain/*.rs`) — Focused service modules containing all business logic. Pure functions where possible, explicit dependencies where not. Target: 20-30 lines per function.
3. **Repository layer** (`repo/*.rs`) — Data access. SQLite queries, file system operations. No business logic.

The decomposition was driven by `stream_commands.rs` growing to 2,425 lines — a single file containing streaming orchestration, tool execution, system prompt building, and session titling. This was extracted into four domain modules: `tool_executor`, `system_prompt`, `stream_loop`, and `session_title`.

## Rationale

Monolithic command files violate the composability principle [AD-1d928079](AD-1d928079). When all logic lives in one file:
- Functions can't be reused across commands
- Testing requires spawning the Tauri app context
- Code reviews are unfocused (everything in one diff)
- Parallel agent work causes merge conflicts

The domain service pattern makes each concern independently testable, composable, and maintainable. The command layer becomes a thin adapter between IPC and domain logic.

## Consequences

- Every command file has a maximum complexity budget — when it exceeds ~300 lines, domain logic must be extracted
- Domain services are framework-free (no Tauri dependency) and can be unit tested without an app context
- New features add domain services, not command code
- The extraction pattern was applied across the codebase: `setup_commands` → `domain::setup`, `governance_commands` → `domain::governance_analysis`, `artifact_commands` → `domain::artifact_reader`

## Git Evidence

- `7fd306e` — Decompose stream_commands.rs into domain modules
- `35b6f76` — Extract setup_commands domain logic
- `e55dd76` — Extract governance_commands domain logic
- `8750420` — Extract artifact_commands helpers to domain/repo modules
- `b71702c` — Decompose stream_send_message into focused helpers
- `c60b181` — Move project_settings I/O to repo layer
- `e7d4d99` — Extract artifact reading logic to domain::artifact_reader
